You are a Senior Full-Stack Architect & Frontend Specialist (Next.js App Router, TypeScript, Tailwind CSS, Shadcn UI).

I am building a multi-vendor eyewear marketplace called "Eynək.com" where various optical stores can list and sell their frames (sunglasses & optical frames) with an integrated Virtual Try-On (AR) feature.

Please generate the frontend architecture and key UI components for this platform.

---

### CORE MARKETPLACE REQUIREMENTS

1. Multi-Vendor System:
   - Vendor Storefront Page (`/store/[vendor-slug]`): Display store details, rating, location, and vendor-specific product catalog.
   - Vendor Badges: Product cards and PDPs must clearly display the seller info (e.g., "Optika Nərgiz • Bakı").

2. Product Catalog & Visual Filters:
   - Visual Frame Shape Filters: Interactive icons for Aviator, Cat-Eye, Rectangle, Round, Square, Wayfarer.
   - Category & Attribute Filters: Gender (Men, Women, Kids), Type (Sunglasses, Optical), Material, Price Range, and Frame Size (S, M, L).
   - Search & Sorting: Fast filtering by brand, store, price, and popularity.

3. Product Detail Page (PDP):
   - Image gallery/carousel with high-resolution frame shots.
   - Prominent "3D Virtual Try-On" trigger button with an AR indicator badge.
   - Detailed Frame Specifications Table (Lens Width, Bridge Width, Temple Length, Frame Material).
   - "Add to Cart" and "Buy Now" CTA buttons (Handling vendor-specific inventory).

4. Virtual Try-On (VTO) Modal UI:
   - Floating AR Viewport Overlay (camera preview container).
   - Bottom horizontal glasses switcher to swap frames instantly during live sınaq.

---

### DELIVERABLES NEEDED

1. Directory Structure: Clean Next.js App Router layout.
2. TypeScript Types (`types/marketplace.ts`): Type definitions for `Product`, `Vendor`, `Category`, `FilterState`, and `CartItem`.
3. Key Components Code:
   - `ProductCard.tsx` (with vendor badge and 3D Try-On button).
   - `VisualFilterSidebar.tsx` (shape icons, category toggles).
   - `VendorStorePage.tsx` (the shop layout for individual sellers).
   - `VirtualTryOnModal.tsx` (the AR overlay component).